		string is_palindrome(int n)
		{
		    // Code here.
		    int tmp = n;
		    int rev = 0;
		    while(n){
		        int dig = n % 10;
		        n /= 10;
		        rev = rev*10 + dig;
		    }
		    
		    if(rev == tmp){
		        return "Yes";
		    }
		    else
		        return "No";
		}
